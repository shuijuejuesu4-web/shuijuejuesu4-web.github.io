---
title: "MIT 6.1200J: 第21讲 — 随机变量"
description: "第21讲：随机变量（Random Variables）"
publishDate: 2026-05-15
tags: [离散数学, 概率, 随机变量, 独立性, 概率质量函数, 累积分布函数, 二项分布, MIT 6.1200J, 笔记, CS110, 讲义]
category: "CS110-讲义"
draft: false
comment: true
---

# 第21讲：随机变量（Random Variables）

:::tip
随机变量是连接概率论与统计学的桥梁。它把抽象的「样本空间中的结果」映射为具体的数值，让你可以计算期望、方差、分布。如果你以后做机器学习或数据分析，每天都会和随机变量打交道——从线性回归到神经网络，底层都是随机变量的运算。
:::

## 1 随机变量

**定义 1**：一个随机变量（random variable, RV）是一个全函数，其定义域为样本空间。

例如，假设样本空间对应掷三枚公平独立硬币。一些可能的 RV：
1. 第一枚硬币的值（H 或 T，或编码为 0/1）
2. 正面的数量（记作 $R$）
3. 如果三枚硬币结果全部匹配则为 1，否则为 0（记作 $M$）——取值为 0/1 的 RV 称为指示变量（indicator）

RV 自然地产生**事件**：对 RV $f$ 和值 $x$，我们定义事件 $f = x$ 为满足 $f(\omega) = x$ 的结果 $\omega$ 的集合。反之，每个事件 $A$ 对应于一个指示 RV $1[A]$，当 $\omega \in A$ 时等于 1，否则为 0。

也可以定义事件 $f \geq x$，以及更一般地 $f \in T$：

$$\Pr[f \in T] = \sum_{x \in T} \Pr[f = x].$$

## 2 条件化与独立性

这些事件可以像任何其他事件一样被条件化。例如：

$$\Pr[R = 2 \mid M = 1] = \frac{\Pr[R = 2 \cap M = 1]}{\Pr[M = 1]} = 0.$$

**定义 2**：两个 RV $X, Y$ 是独立的（independent），如果对所有值 $x, y$，$\Pr[X = x \cap Y = y] = \Pr[X = x] \cdot \Pr[Y = y]$。等价地，对所有 $x, y$，要么 $\Pr[Y = y] = 0$，要么 $\Pr[X = x \mid Y = y] = \Pr[X = x]$。

直观理解：两个 RV 独立，如果知道 $Y$ 取任何特定值都不会揭示关于 $X$ 的额外信息。

**示例**：
- $R$ 和 $M$ **不独立**（$\Pr[R = 2 \cap M = 1] = 0$ 但 $\Pr[R = 2] \neq 0$ 且 $\Pr[M = 1] \neq 0$）
- 掷两枚公平骰子得 $D_1, D_2$，令 $S = D_1 + D_2$。$D_1$ 和 $S$ 不独立。但令 $T = 1[S = 7]$，则 $T$ 和 $D_1$ **是独立的**！（因为对 $D_1$ 的每个值，恰有 $D_2$ 的一个值使和为 7）

**定义 3**：一组 RV $X_1, \ldots, X_n$ 是相互独立的（mutually independent），如果对所有值 $x_1, \ldots, x_n$，

$$\Pr[X_1 = x_1, \ldots, X_n = x_n] = \Pr[X_1 = x_1] \cdots \Pr[X_n = x_n].$$

## 3 分布、PMF 和 CDF

对任意 RV $X$，定义概率质量函数（probability mass function, PMF）：

$$f(x) = \Pr[X = x],$$

以及累积分布函数（cumulative distribution function, CDF）：

$$F(x) = \sum_{y \leq x} \Pr[X = y].$$

常见情况：
- **伯努利（Bernoulli）/ 指示随机变量**：$f(0) = p, f(1) = 1-p$
- 均匀随机变量（uniform random variable）在 $\{1, 2, \ldots, n\}$ 上：$f(i) = 1/n, F(i) = i/n$

## 4 两信封问题（Two Envelope Problem）

我准备两个信封，每个装有 0 到 100 美元之间的一个未知整数（且两数不等）。你选一个信封。你选到金额更大的信封的概率是多少？显然不超过 $1/2$。

现在，我让你偷看你所选信封里的金额，然后给你机会换信封。你应该换吗？如果总做相同的事（总换或总不换），成功率都不能超过 $1/2$。

**策略：如果你碰巧知道一个数 $z$ 位于两个信封金额的中间，则策略很明确：如果你看到的金额高于 $z$ 就不换，否则换！但你如何得知 $z$？猜！**

令 $z$ 为从 $\{0.5, 1.5, \ldots, 99.5\}$ 中均匀随机选取的阈值。策略：如果 $x_r > z$ 则不换，否则换。

分析：如果 $z$ 介于 $x_0$ 和 $x_1$ 之间，我们一定成功。否则，我们以概率 $1/2$ 成功（此时我们总是换或总是不换）。对任意固定的 $x_0, x_1$，至少存在 1 个 $z$ 的值满足条件（概率至少 $1/100$）。故成功概率至少为

$$\frac{1}{100} \cdot 1 + \frac{99}{100} \cdot \frac{1}{2} = \frac{1}{2} + \frac{1}{200}.$$

比随机猜测更好！这里我们用概率来**设计算法**以解决一个任务，而不仅仅是建模世界中的不确定性。

## 5 二项分布（Binomial Distribution）

CS 中极其常见的概率分布。有两个参数：$n$ 和 $p$。模型：
1. 掷 $n$ 枚独立硬币，每枚以概率 $p$ 出现正面。计数正面数量的 RV $X$ 是二项的。
2. $n$ 个组件，每个以概率 $p$ 发生故障。计数故障数量的 RV $X$ 也是二项的。

PMF：

$$f_{n,p}(k) = \Pr[X = k] = \binom{n}{k} p^k (1-p)^{n-k}.$$

直觉：每个恰好有 $k$ 个正面的结果以概率 $p^k (1-p)^{n-k}$ 发生，共有 $\binom{n}{k}$ 个这样的结果。

CDF：

$$F_{n,p}(k) = \sum_{j=0}^{k} \binom{n}{j} p^j (1-p)^{n-j}.$$

使用 Stirling 近似，对 $f_{n,p}(\alpha n)$ 可得到：

$$f_{n,p}(\alpha n) \sim \frac{1}{\sqrt{2\pi\alpha(1-\alpha)n}} \cdot 2^{(\alpha \log \frac{p}{\alpha} + (1-\alpha) \log \frac{1-p}{1-\alpha})n}.$$

指数项总是负的（除非 $p = \alpha$）。最大值出现在 $p = \alpha$ 处，其他地方呈指数级减小。

对 $p = 0.5, n = 100$：$f(50) \approx 0.08$，而 $f(25) \approx 1.9 \cdot 10^{-7}$。分布的"尾部"非常小。
